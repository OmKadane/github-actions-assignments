{
    "name": "backend",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",
      "test": "echo 'Backend tests passed!' && exit 0",
      "build": "echo 'Building backend...' && mkdir -p dist && cp server.js dist/server.js"
    }
  }