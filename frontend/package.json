{
    "name": "frontend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
      "lint": "echo 'Linting complete.' && exit 0",
      "test": "echo 'Frontend tests failed!' && exit 1",
      "build": "echo 'Building frontend...' && mkdir -p dist && cp index.js dist/index.js"
    }
  }